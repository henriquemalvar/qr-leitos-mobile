import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  scanner: {
    borderRadius: 20,
    height: 300,
    overflow: "hidden",
    width: 300,
  },
});

export default styles;
