import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  
  container: {
    alignItems: "center",
    flexDirection: "row",
    paddingLeft: 10,
    paddingTop: 10,
  },
  text: {
    fontSize: 32,
    fontWeight: "bold",
    paddingLeft: 50,
  },
});
