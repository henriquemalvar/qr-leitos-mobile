import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  containerStatus: {
    paddingTop: 10,
    flex: 1,
    color: "#ffffff",
    alignItems: "center",
    marginBottom: 110,
  },
});

export default styles;
